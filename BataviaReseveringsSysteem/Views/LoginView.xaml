<UserControl x:Class="Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Views"
        mc:Ignorable="d"
        d:DesignWidth="640" d:DesignHeight="480" Width="800" Height="600">
    <Grid x:Name="loginLayout" Background="White">
        <Label Content="" HorizontalAlignment="Left" Margin="82,43,0,0" VerticalAlignment="Top"/>
        <Label Content="Lidnummer" HorizontalAlignment="Left" Height="31" Margin="41,25,0,0" VerticalAlignment="Top" Width="112"/>
        <Label Content="Wachtwoord" HorizontalAlignment="Left" Height="23" Margin="310,25,0,0" VerticalAlignment="Top" Width="95"/>
        <TextBox x:Name="Username" HorizontalAlignment="Left" Height="23" Margin="139,25,0,0" TextWrapping="Wrap"  VerticalAlignment="Top" Width="125" PreviewTextInput="NumberValidationTextBox"  />
        <PasswordBox x:Name="Password" PasswordChar="*" HorizontalAlignment="Left" Height="23" Margin="410,25,0,0"  VerticalAlignment="Top" Width="125"/>
        <Button Content="Login" IsDefault="True"  HorizontalAlignment="Left" Margin="576,23,0,0" VerticalAlignment="Top" Width="125" Click="LoginButton" Height="25"/>
        <Label x:Name="Test" Content="" HorizontalAlignment="Left" Height="42" Margin="332,275,0,0" VerticalAlignment="Top" Width="113"/>
        <Label Content="" x:Name="LoginError" HorizontalAlignment="Left" Margin="332,43,0,0" Foreground="Red" VerticalAlignment="Top"/>
        <Button  Content="Registreren" Visibility ="Hidden" HorizontalAlignment="Left" Margin="282,245,0,0" VerticalAlignment="Top" Width="123" Click="RegistrerenButton" Height="25"/>

        <DataGrid x:Name="DataReservations"  CanUserAddRows="false" x:FieldModifier="public"  AutoGenerateColumns="false" HorizontalAlignment="Left"  Margin="41,74,0,0" VerticalAlignment="Top" >
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="BoatName" Width="auto"  Binding="{Binding Boat.Name}"    Header="Bootnaam" IsReadOnly="true" />
                <DataGridTextColumn x:Name="BoatType" Binding="{Binding Boat.Type}" Header="Type" IsReadOnly="true"/>
                <DataGridTextColumn x:Name="NumberOfRowers"  Binding="{Binding Boat.NumberOfRowers}" Header="Aantal roeiers" IsReadOnly="true"/>
                <DataGridTextColumn x:Name="Date"  Binding="{Binding Start.Date}" Header="Datum" IsReadOnly="true"/>
                <DataGridTextColumn x:Name="DateStart" Width="auto"    Binding="{Binding Start}" Header="Starttijd" IsReadOnly="true" />
                <DataGridTextColumn x:Name="DateEnd"    Binding="{Binding End}" Header="Eindtijd" IsReadOnly="true"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>

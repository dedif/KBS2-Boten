<UserControl x:Class="BataviaReseveringsSysteem.Views.AddNewsMessage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BataviaReseveringsSysteem.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <TextBox   x:Name="TitleBox" 
                materialDesign:HintAssist.Hint="Titel"
                 VerticalAlignment="Top" HorizontalAlignment="Left" Width="120" Height="25" Margin="10,59,0,0" />
        <TextBox
                x:Name="NewsMessageBox"
                Style="{StaticResource MaterialDesignOutlinedTextFieldTextBox}"
                VerticalAlignment="Center"
                Height="100"
                AcceptsReturn="True"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto"
                materialDesign:HintAssist.Hint="Bericht"
                IsEnabled="{Binding Path=IsChecked, ElementName=MaterialDesignOutlinedTextFieldTextBoxEnabledComboBox}" Margin="0,153,0,197" />
        <Label  x:Name="NotificationLabel" Content="&#xD;&#xA;" HorizontalAlignment="Left" Margin="279,59,0,0" VerticalAlignment="Top" Width="290" FontSize="16" Foreground="#DDFF0000"/>
        <Button Content="Opslaan" HorizontalAlignment="Left" Margin="644,384,0,0" VerticalAlignment="Top" Width="86" x:Name="SaveNewsMessage" Click="SaveNewsMessage_Click"/>
        <Button Content="Ga terug" HorizontalAlignment="Left" Margin="44,384,0,0" VerticalAlignment="Top" Width="86" x:Name="Cancel" Click="CancelNewsMessage_Click"/>
    </Grid>
</UserControl>
